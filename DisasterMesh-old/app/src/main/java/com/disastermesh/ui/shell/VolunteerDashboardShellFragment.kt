package com.disastermesh.ui.shell

import androidx.fragment.app.Fragment
import com.disastermesh.R

/**
 * Prototype shell for the volunteer task dashboard from prototype/index.html.
 *
 * This is intentionally view-only for now so the current volunteer mesh flow remains untouched.
 */
class VolunteerDashboardShellFragment : Fragment(R.layout.fragment_volunteer_dashboard_shell)
