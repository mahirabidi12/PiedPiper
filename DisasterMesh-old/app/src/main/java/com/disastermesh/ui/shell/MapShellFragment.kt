package com.disastermesh.ui.shell

import androidx.fragment.app.Fragment
import com.disastermesh.R

/**
 * Future host for the prototype map tab.
 *
 * Wire OSMDroid and the role-based visibility rules here when the navigation host is added.
 */
class MapShellFragment : Fragment(R.layout.fragment_map_shell)
